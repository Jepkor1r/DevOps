apiVersion: v1
kind: Service
metadata:
  name: result-service
  labels:
    name: result-service
    app: project-voting-app
spec:
  selector:
    name: result-app-pod
    app: project-voting-app
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30007