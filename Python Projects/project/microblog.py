# This is the main entry of the application
# Import the Flask application instance `app` from the `app` package and start the Flask development server when the script executed
from app import app

app.config['SECRET_KEY']
'you-wil-never-guess'